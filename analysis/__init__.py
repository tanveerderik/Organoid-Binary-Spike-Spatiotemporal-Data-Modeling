"""Package marker for `analysis`.

Present so `MAGVIT_project.analysis.*` resolves as a regular package like
every other package here, rather than relying on PEP-420 namespace-package
fallback the way it did before.
"""
